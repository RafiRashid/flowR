# Suppress summarise info
options(dplyr.summarise.inform = FALSE)

library(shiny) # build interactive web apps in R
library(bslib) # bootstrap library for Shiny
library(bsicons) # bootstrap icons
library(shinyWidgets) # collection of custom inputs widgets for Shiny (from dreamRs)
library(fst) # fast serialization dataframe (fast writing and reading dataframes)
library(dplyr) # data manipulation
library(tidyr) # tabular manipulation, standard way to organise data
library(echarts4r) # better interractive graphs
library(reactable) # interactive data tables for R, based on the React Table library and made with reactR.
library(sass) # custom styling with CSS
